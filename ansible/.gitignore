# ============================================================
# ANSIBLE .gitignore - Protect Sensitive Files
# ============================================================
# This file tells Git which files to NEVER commit to the repository
# Critical for security - prevents exposing secrets
# ============================================================

# ------------------------------------------------------------
# VAULT PASSWORD FILE - CRITICAL!
# ------------------------------------------------------------
# Contains the password to decrypt vault.yml
# If committed, anyone can decrypt your secrets!
.vault_pass.txt
vault_pass.txt
.vault_password
vault_password

# ------------------------------------------------------------
# ANSIBLE RETRY FILES
# ------------------------------------------------------------
# Created when playbooks fail (lists failed hosts)
# Not needed in version control
*.retry

# ------------------------------------------------------------
# ANSIBLE FACTS CACHE
# ------------------------------------------------------------
# Cached system information from managed hosts
# Regenerated automatically, doesn't need versioning
facts/
fact_cache/
*.fact

# ------------------------------------------------------------
# PYTHON BYTECODE
# ------------------------------------------------------------
# Compiled Python files (if using custom modules)
*.pyc
*.pyo
__pycache__/

# ------------------------------------------------------------
# EDITOR TEMPORARY FILES
# ------------------------------------------------------------
# Backup and swap files from editors
*~
*.swp
*.swo
*.swn
.*.sw?
*.bak
*.backup

# ------------------------------------------------------------
# OS-SPECIFIC FILES
# ------------------------------------------------------------
# Windows
Thumbs.db
Desktop.ini

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Linux
.directory

# ------------------------------------------------------------
# IDE/EDITOR FOLDERS
# ------------------------------------------------------------
.vscode/
.idea/
*.iml

# ------------------------------------------------------------
# LOGS AND TEMPORARY FILES
# ------------------------------------------------------------
*.log
*.tmp
tmp/
temp/

# ------------------------------------------------------------
# ANSIBLE COLLECTIONS (if using collections)
# ------------------------------------------------------------
collections/
*.tar.gz

# ------------------------------------------------------------
# WHAT IS SAFE TO COMMIT:
# ------------------------------------------------------------
# ✅ ansible.cfg - Configuration (no secrets)
# ✅ inventory.ini - Server list (IPs are public anyway)
# ✅ playbooks/*.yml - Automation scripts (no secrets)
# ✅ group_vars/all/vars.yml - Public variables
# ✅ group_vars/all/vault.yml - ENCRYPTED secrets (safe when encrypted)
# ✅ README.md - Documentation
#
# ❌ .vault_pass.txt - NEVER COMMIT THIS!
# ❌ *.retry - No need to commit
# ❌ *.log - No need to commit
# ============================================================
