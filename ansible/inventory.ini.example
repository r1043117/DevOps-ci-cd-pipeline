# ============================================================
# ANSIBLE INVENTORY FILE - EXAMPLE
# ============================================================
# Copy this file to inventory.ini and fill in your values:
#   cp inventory.ini.example inventory.ini
#
# Get IP addresses from: terraform output
# ============================================================

[app_servers]
vm1 ansible_host=YOUR_APP_SERVER_IP ansible_user=admin ansible_ssh_private_key_file=~/.ssh/your-key.pem

[jenkins_servers]
vm2 ansible_host=YOUR_JENKINS_SERVER_IP ansible_user=admin ansible_ssh_private_key_file=~/.ssh/your-key.pem

# ============================================================
# USAGE:
# ============================================================
# 1. Copy this file:
#    cp inventory.ini.example inventory.ini
#
# 2. Get your IPs from Terraform:
#    cd ../terraform && terraform output
#
# 3. Replace placeholders:
#    - YOUR_APP_SERVER_IP -> app_server_public_ip from terraform output
#    - YOUR_JENKINS_SERVER_IP -> jenkins_server_public_ip from terraform output
#    - your-key.pem -> your SSH key filename
#
# 4. Test connection:
#    ansible all -m ping
# ============================================================
